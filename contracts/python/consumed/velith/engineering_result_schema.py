# @generated by tools/generate_contracts.py from contracts/schemas/ — DO NOT EDIT (repo-arch §13).

from __future__ import annotations

from dataclasses import dataclass
from typing import Literal

from ... import common_schema


@dataclass
class DesignArtifact:
    media_type: str
    ref: common_schema.Uuid
    content_hash: common_schema.ContentHash


@dataclass
class EngineeringResult:
    """
    CONSUMED STUB — owned by Velith. RM1 reads result_id, content_hash, verified, and the OPAQUE design_artifact only; it NEVER parses the artifact and NEVER re-verifies (Law 6/9). Pinned until the real Velith contract publishes; the integrations/velith adapter maps real -> this stub. The velith_verified DesignInput path requires verified == true.
    """

    result_id: common_schema.Uuid
    content_hash: common_schema.ContentHash
    verified: Literal[True]
    verifier: common_schema.ProducedBy
    design_artifact: DesignArtifact
